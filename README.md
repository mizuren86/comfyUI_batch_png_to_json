# comfyUI_batch_png_to_json
batch change the spec strings png to the json

# png
the code read the metadata of the comfyUI original png file, it's a json
then use this code to batch chanage the prompt
